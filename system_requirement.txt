--1. Make sure at least python version 3 is installed in your system.
   To check type python --version in command prompt. 
   Check if is python version 64bit or not. Only install 64bit
   version of python if your windows is also 64bit.

Steps:
Install Python
Install Pip
Install VirtualEnv
Install VirtualEnvWrapper-win

2. Open project file in command prompt.e.g. "cd documents/GPDC".

3. Import sql file using "http://localhost/phpmyadmin/"(make sure your using xampp).
file name (gpdc.sql)


3. Install Flask. By typing "pip install flask" on command prompt.

4. Install other flask dependencies like wtforms, flask_mysqldb,
   passlib.Theirs commands are "pip install wtforms",
   "pip install flask_mysqldb","pip install passlib".

5. Check python version(the first point 1.) if any dependency fails.

6. Type in command prompt "python app.py" in project directory(the second point 2.) to run the project.

7. pip install email_validator

8. pip3 install tensorflow==2.4.1

9. pip3 install keras==2.4.0

10. pip install pdfkit

11. pip install wkhtmltopdf




	
 	